export default class Categories {
    constructor(id, categoryID, name, icon, iconType) {
        this.id = id;
        this.categoryID = categoryID;
        this.name = name;
        this.icon = icon;
        this.iconType = iconType;
    }
}